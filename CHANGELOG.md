# 变更日志（Changelog）

本项目所有版本记录遵循 [语义化版本 2.0.0](https://semver.org/lang/zh-CN/)：`MAJOR.MINOR.PATCH`。
版本号唯一来源为根目录 `VERSION` 文件，Git 标签为 `v<MAJOR>.<MINOR>.<PATCH>`。
发布流程与版本规则见 [README.md](README.md) §15。

---

## [v1.0.0] - 2026-09-16 00:35

首个公开版本。纯标准库（Python ≥ 3.8）实现的 PT（NexusPHP 系）保种组工资结算与赠送发放工具，
含 tkinter 图形界面与 7 套离线自测；零第三方依赖，整个目录拷到任何装了 Python 的机器上直接能跑。

### 新增

- **考核与结算**：读组长自己维护的 `.xlsx` / `.csv` 考核表（按表头名认列，列顺序随便动），
  `.xlsx` 按内容识别扩展名；「检查日期」支持复合列（`日期-体积` / `日期-数量` / 两者并存）；
  考核口径 `metrics` 单选或多选，输出列跟着口径变。
- **联网刷新**：默认 4 路并发（`measure_workers`，1 = 串行），逐人进度回调、边抓边刷；
  瞬时失败自动重试（1 次原始 + 2 次重试，4xx 不重试）。
- **工资表**：列顺序对齐考核表（前 4 列原样保留），结果列为计算结果而非照抄；
  `utf-8-sig` 编码 WPS / Excel 直接开，另可制表符导出直接粘进表格。
- **发放**：先出 dry-run 清单、人工核对，再真实发放；每批发放前**现抓**赠送表单的
  `action` 与隐藏字段（不写死，兼容二次开发的站）；按站点节奏限速。
- **图形界面**（tkinter，四个页签）：站点与登录 / 考核与方案 / 算工资 / 发放与台账；
  两级表格三态行色（达标 / 不达标 / 未测），「状态」「结果」列宽按内容自适应，
  周期与发放次数**边打边生效**，配置改动自动写回 `config.json`（JSONC，注释一行不丢）；
  两个作业页的过程行各归各页，互不串显；① 页顶部带可点击的开源地址。
- **首次运行自举**：缺 `config.json` / `roster.csv` 时自动从各自的 example 生成
  （幂等，日志里各提示一行）。
- **roster.csv 即缓存**：每次算完把实测结果（用户名 / 检查日期）按 UID **写回考核表**
  （保原编码、抓失败的行不覆盖原值）；③ 表格支持**双击就地编辑**方案 / 用户名 / 检查日期，
  外部改完考核表点「重新载入」同步进来（程序不锁文件）。
- **命令行**：`login.py` / `probe.py` / `settle.py` / `payout.py` / `ledger.py` 各自可独立使用，
  与界面共用同一份代码与同一份台账。
- **离线自测**：7 套、合计 **557 条断言**，全部不碰真实站点。

### 安全

- **限额一条就够**：每人每周期最多 N 次（界面上可改），每笔发出**前**数台账
  （`ledger.times_of`），发满自动跳过 —— 重跑 / 手抖重点 / 断线续发都不会多发；
  不设其他防重层。
- **成功判定只看落点 URL**（成功跳 `do=transfer`、重复提交跳 `do=duplicated`），
  页面文字一律不看；判不出来按「没送出去」处理，宁可少发、绝不误发。
- **只有「重复提交」才自动重试**，网络中断 / 判不出落点 / 登录态失效一律不重试
  （这些情况都可能已经提交成功，重试就是重复发钱）。
- **失败也记账但不占次数**，重跑即可补发；「人收到了、工具判失败」用 `--mark-paid <uid>` 补记，
  绝不直接重发。
- **重置周期只追加 `reset` 作废记录**，台账历史一条不删。
- **凭据与真实数据隔离**：`config.json`（含 cookie）、台账、真实名单、运行日志、响应快照
  全部在 `.gitignore` 内；仓库内不含任何真实站点域名、UID、用户名。
